import { HStack, VStack } from "@chakra-ui/react"

export const Footer = (): React.ReactElement => {
  return (
    <HStack bg={"purple.500"} color={"white"}>
      <VStack></VStack>
    </HStack>
  )
}
